#pragma once

#include <stddef.h>
#include <stdint.h>

void hex_dump_addr(const size_t len;
		const uint8_t tmp[static len],
		const size_t len,
		size_t llen,
		const size_t addr);
void hex_dump(const size_t len;
		const uint8_t tmp[static len],
		const size_t len,
		size_t llen);
